entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

certificatesResolvers:
  letsEncrypt:
    acme:
      email: faizanops@alazka.ai
      storage: /acme.json
      tlsChallenge: {}

providers:
  docker:
    exposedByDefault: false
  file:
    filename: /home/azureuser/traefik-all-static/dynamic.yml
